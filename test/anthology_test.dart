import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder smoke test: proves the test harness itself runs for this
  // package (flutter test discovers the file, flutter_test resolves). The file
  // was named jotrockenmitlockenrepo_test.dart until 2026-09-06 — a leftover
  // from the repo this package was split out of — and its title claimed an
  // arithmetic behaviour it never tested. Replace with real widget tests as
  // the package grows behaviour worth pinning.
  test('test harness runs for package anthology', () {
    expect(1, 1);
  });
}
